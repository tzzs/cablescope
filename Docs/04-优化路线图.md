# 优化路线图（对标 whatcable）

> 2026-09-13 定稿、2026-09-20 清理。背景：对标 [whatcable](https://github.com/darrylmorley/whatcable)（macOS USB-C 检测工具）后的差距分析与优化计划；产品决策为**全量开源、无付费版、双渠道分发**。
>
> 本文只保留**仍需推进**的内容。已完成里程碑压缩为下面的速览表——实现细节见 git history（按里程碑日期查），长期有效的设计约束已沉淀到 `AGENTS.md`（分层规则、CableMonitor 事件模型、`TTLCache`、本地化红线、文案红线）与各文件的头部注释。

## 一、产品与分发模型（定稿）

- **不做付费版**：全功能开源（MIT，避开 GPL×MAS 不兼容红线），可持续性走可选捐赠。
- **双渠道同一代码库**：差异只来自 Apple 硬规则，不人为阉割——
  - **外部版**（主渠道）：全功能含 CLI；Homebrew 双包 —— cask `cablescope` 装 App（需公证）、formula `cablescope-cli` 装 `cablescope` 命令（源码构建，不需签名，因此不受 Apple 证书进度阻塞）/ GitHub Releases notarized DMG。
  - **商店版**（可选、低优先）：MAS 上架，受规则自然受限——不带 CLI（Guideline 2.4.5）、必须 App Sandbox。Gate A 已验证 AppleHPM 读取在 sandbox 下可用，端口层数据双版本通用。
- 评级引擎、功率历史、插拔通知等功能**不做任何版本/付费门控**。

## 二、已完成里程碑速览

| 里程碑 | 日期 | 内容 |
| --- | --- | --- |
| M0 基础验证与重构 | 2026-09-13 | Gate A（sandbox 下 AppleHPM/SOP' 读取验证通过）；RatingStore 统一为单一 `ratings.json` + 旧文件幂等迁移 |
| M1 端口控制器数据层 | 2026-09-13 | `USBCPortSnapshot`/`EMarkerSnapshot`/`PDOPortPowerSnapshot` 模型；`PortControllerService`（AppleHPM/AppleTC 直读 + Cable VDO 解码）；USB 设备↔物理端口配对；接入 CableMonitor/UI/CLI |
| M2 诊断解释层 + 通知 | 2026-09-13 | `DiagnosticsEngine`（充电归因、端口头条，守文案红线）；多线缆 per-cable 卡片；插拔通知 |
| M3 数据增强 | 2026-09-14 | `usb-vendors.json` + `VendorDirectory`；`EMarkerTrust` 可信度信号；雷雳代际识别 |
| M4 工程升级 + Widget | 2026-09-14 | xcodegen 工程（三 target/三 config）；entitlements 文件化；WidgetKit 扩展；App 图标三路接入 |
| M5 开源分发 | 2026-09-14 | MIT/CONTRIBUTING/PRIVACY；CI + release workflow；Homebrew formula/cask；`UpdateChecker`；CableMonitor 事件驱动采样 |
| M6 待办清偿 | 2026-09-14 | TB5 非对称 120G 误判修复；图标 Xcode 路径补齐；雷雳 dock 拓扑深度缩进；MAS 隐私材料 |
| M7 设置页 + 语言/主题 | 2026-09-16 | Settings scene；语言切换全覆盖（`KitLocalization`/`AppLocalization` 显式 locale）；主题切换；通知扩展为五类事件 + 分类开关 |
| M8 工程质量强化 | 2026-09-18 | 新增 `CableScopeAppTests`（App 层此前零测试）；`RegistryService` 补测；`scripts/check_layering.sh` 接入 CI |
| M9 本地化修复 + 常驻开销优化 | 2026-09-18 | 英文表迁经典 `.strings`（修复发布构建下英文界面全中文回退）；`TTLCache` 给 system_profiler 读取加缓存（常驻 CPU 实测 -87%）；视图层纯函数补测 |
| M10 CI 覆盖 Xcode 路径 + Widget 偏好联动 | 2026-09-19 | CI 增加 `xcodegen generate` + `xcodebuild`（Widget Debug / App AppStore config）；Widget 按 App bundle ID 读语言与主题偏好（`SharedAppPreferences`），并补上自己的英文译文表 |

## 三、待推进事项

### A. 发布链路：已跑通，只剩 Apple 公证 secrets

**首次正式发布已于 2026-09-19 完成**：v0.2.0 / v0.2.1 / v0.3.0 / v0.4.0 依次走通 release-please → tag → Publish → Homebrew tap 同步的完整链路。当时的两处链路断点（`skip-github-release`、`include-component-in-tag`）已修复并固化为 `AGENTS.md`「Release process」的红线；废弃的本地 tag `v0.1.0` 已删除，解堵事项已自然完成，此处不再记录过程。

唯一剩余项：**GitHub 仓库尚未配置四项 Apple 开发者 secrets**——`APPLE_ID` / `APPLE_TEAM_ID` / `APPLE_APP_SPECIFIC_PASSWORD` / `DEVELOPER_ID_APPLICATION`。当前表现：

- 每个 Release 挂的都是未公证 DMG，正文自动追加双语未公证提示（首次打开需用户在「系统设置 → 隐私与安全性」放行）。
- 未公证 DMG 被 `release.yml` 显式挡在 tap 同步之外，所以 tap 里没有 `Casks/` 目录——`brew install --cask tzzs/tap/cablescope` 至今装不了。两份 README 已注明"cask 仅在存在公证构建后发布"并引导使用 `cablescope-app`（源码构建、免签名），不会误导用户，但这条路在 secrets 配好前仍是空头承诺。

配好 secrets 后无需补发旧版本——下一次常规发版即产出公证 DMG 并同步 cask；若确需给已发布版本补公证，用 `workflow_dispatch` 指定 tag 手动重发（护栏见 `AGENTS.md`）。

### B. 代码已就位，等真机/日常使用验证

- **Widget 桌面渲染实弹**：真机添加 Widget 需有效签名，待签名 App 日常使用中观察。M10 的语言/主题联动改动同样只做到「编译通过 + 偏好读取逻辑有单测」，实际桌面渲染效果（尤其 `\.colorScheme` 在 WidgetKit 下是否被宿主尊重）要在这一步一并确认。
- **插拔通知横幅实弹**：同上。
- **TB5 非对称 120G 代际识别复核**：M6 已把映射从子串匹配改为"提取数值取最大"并有单测覆盖，但真机复核要等手头有 TB5 非对称外设。

### C. 需要 Apple Developer 后台配置

- **Widget 偏好联动的沙盒补丁**：M10 让 Widget 按 App 的 bundle ID 读语言/主题偏好，这在外部分发渠道（两端都不带沙盒）下可用且无需 App Group；但**沙盒的 MAS 构建下读不到**，会安全退回跟随系统。真要在 MAS 版也联动，需要 App Group + `UserDefaults(suiteName:)`（新增 entitlements + Apple Developer 后台注册 App Group）——只在推进 MAS 上架时才值得做。
- **MAS 提审**：材料已就绪（`Packaging/privacy/PrivacyInfo.xcprivacy`、`Packaging/entitlements/mas.entitlements`，sandbox 端到端验收已通过），提审本身未做。定位为可选、低优先。

### D. 外部数据阻塞（含集成设计与触发条件）

这三项都不是能力问题，而是拿不到可靠数据源。**设计已预研，数据到位前不引入无数据可测的死代码。**

**XID 认证核查**
- 数据源：USB-IF XID 认证清单，离线获取未果，待社区提供。
- 集成设计：`usb-vendors.json` 条目扩展可选 `xid` 字段；`EMarkerSnapshot` 的 VID + productType 与清单匹配后，由 `EMarkerTrust` 新增 note case（如 `.certifiedMatch`）渲染"认证在册"提示——措辞仍守红线：**在册 ≠ 真伪判决**。

**dock 中间 hub 身份标注**
- 已落地：雷雳子树深度缩进（`ThunderboltDeviceSnapshot.depth`）。
- 剩余：链路中间 hub 的身份标注（名称/端口）——`system_profiler` 不暴露中间 hub 的稳定标识，待找到可靠数据源。

**线缆电阻估算**
- 估算需"合同电压 − 远端电压 = 压降"，而：远端（设备侧）电压 macOS 不暴露；顶层 `Amperage` 是**整机**抽流非端口值（多线时无法归属到线）；PPS/AVS 原始报文缺失（`PDOPortPowerSnapshot` 仅平铺档位）；电池侧微小放电会混入电流读数。
- **触发条件**：拿到端口级电流读数或远端电压感知数据（如雷雳坞站电压上报）后重启此项目。

### E. 远期 / 低优先

- **TUI dashboard**：对标项里唯一没做的形态，优先级低（CLI 的 `watch` 已覆盖大部分使用场景）。

## 四、真机踩坑记录（实现时必读，详见 Docs/03）

1. IOPort 节点 `IORegistryEntryGetChildEntry` 返回成功但空迭代器 → 用 `IORegistryEntryCreateIterator` + `kIORegistryIterateRecursively`。
2. `PowerSourceOptions` 是 CFSet（__NSCFSet）不是数组 → `as? NSSet` 桥接，按电压排序展示。
3. VDO Data 是线上小端序 → `UInt32(littleEndian:)`，已与 Apple 平铺字段交叉验证。
4. 内置根 hub 无 `LocationID`/`Speed` 属性 → 路径末段 `@hex` 回退。
5. `IORegistryEntryGetName` 不含 `@N` 位置段 → 端口 ID 用 `IORegistryEntryGetPath` 末段。
6. 端口节点上的 `SOPMfgString`（"Realtek"）是 HPM 控制器芯片厂商（RTBuddy），**不是**线缆 e-marker 厂商；e-marker 厂商看 SOP' 节点的 `Vendor ID`。
7. 经典 SwiftPM 构建引擎（CI 在用）不编译 `.xcstrings`，英文表必须是手写的经典 `.strings`；相关红线见 `AGENTS.md`「Localization」章节。

## 五、对标差距状态总览

| whatcable 能力 | CableScope 状态 |
| --- | --- |
| USB-C 端口控制器直读（AppleHPM/AppleTC） | ✅ |
| e-marker 解码（速度/3A·5A/芯片厂商） | ✅ |
| 充电器 PDO 全列表 + 当前协商档 | ✅ |
| 充电瓶颈归因（线缆/机器/电池） | ✅ |
| 端口一句话头条 | ✅ |
| USB 设备↔物理端口配对 | ✅ |
| 线缆历史（多线缆、拔线不清零） | ✅ 评级引擎 + 会话卡片 |
| 插拔通知 | ✅ 五类事件 + 分类开关 |
| VID 数据库 / 可信度信号 | ✅ |
| 桌面 Widget | ✅ 含 App 语言/主题联动（沙盒 MAS 构建除外，见三·C） |
| TB 代际（TB3/4/5） | ✅ 含 120G 非对称识别；dock 拓扑深度已落地 |
| 线缆电阻估算 | ⏳ 数据缺口，见三·D |
| TUI dashboard | ⏳ 远期，见三·E |
| CLI（json/watch/report） | ✅ snapshot/pretty/watch/rating/properties/throughput |
| 我们独有 | 通用 IORegistry 属性检查器、实时功率曲线、电池循环计数、macOS 14+ / Intel 降级支持、全量开源（MIT） |
